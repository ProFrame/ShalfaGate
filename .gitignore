# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.env
.env.*
!.env.example
*.local
scratch_keys.txt
tmp-*-import.xlsx

# Planning documents. thirdupdate.md contains a mail account password in plain
# text, so it must not reach a commit. Remove this line once the credential has
# been taken out of the file and the account password rotated.
thirdupdate.md
# Same policy for every future numbered plan: local-only by default, reviewed
# before it is ever deliberately un-ignored.
FourthUpdate.md

# Local scratch artifacts (temp builds, test uploads) — never part of a commit.
.tmp-build-*/
.tmp-build-*.zip
.tmp-signature-*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
